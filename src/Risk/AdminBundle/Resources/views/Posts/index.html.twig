{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Posts list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datecreation</th>
                <th>Expediteur</th>
                <th>Type</th>
                <th>Contenu</th>
                <th>Objet</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('Threads_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.dateCreation %}{{ entity.dateCreation|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.expediteur }}</td>
                <td>{{ entity.type }}</td>
                <td>{{ entity.contenu }}</td>
                <td>{{ entity.objet }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('Threads_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('Threads_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('Threads_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
